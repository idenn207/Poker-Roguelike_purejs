class DeckManager extends ManagerCore {
  constructor(eventBus) {
    super();
    this.eventBus = eventBus;
  }

  /** 덱 초기화 */
  init() {}

  /** 덱 이벤트 등록 */
  registerEvents() {}

  update(deltaTime) {}
}

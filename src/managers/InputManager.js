class InputManager {
  constructor(eventBus) {
    this.eventBus = eventBus;
  }

  /**
   * 이벤트 리스너 등록
   */
  init() {
    this.eventBus;
  }

  update() {
    // 입력 상태 업데이트
  }

  /** 클릭된 요소 판단 */
  handleClick(element) {}
}

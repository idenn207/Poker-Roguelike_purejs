class ShopManager extends ManagerCore {
  constructor(eventBus) {
    super();
    this.eventBus = eventBus;
  }

  /** 상점 초기화 */
  init() {}

  /** 상점 이벤트 등록 */
  registerEvents() {}

  update(deltaTime) {}
}

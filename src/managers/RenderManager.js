class RenderManager extends ManagerCore {
  constructor(eventBus) {
    super();
    this.eventBus = eventBus;
  }

  /** 렌더링 초기화 */
  init() {}

  /** 렌더링 이벤트 등록 */
  registerEvents() {}

  update(deltaTime) {}
}

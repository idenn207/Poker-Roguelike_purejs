class CardManager extends ManagerCore {
  constructor(eventBus) {
    super();
    this.eventBus = eventBus;
  }

  /** 카드 매니저 초기화 */
  init() {}

  /** 카드 매니저 이벤트 등록 */
  registerEvents() {}

  update(deltaTime) {}
}
